import AttackPlayer from './AttackPlayer';

class HotGirlPlayer extends AttackPlayer {
  constructor(id, name, health, attack){
    super(id, name, health, attack);
  }
}

export default HotGirlPlayer;
